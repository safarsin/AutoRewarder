# Browser automation: nodriver (Selenium replacement, CDP-based, stealth).
# Pin the exact release after installing once: `pip install nodriver`.
nodriver
pywebview==6.1
requests==2.33.1
pystray==0.19.5
Pillow==10.4.0
nlpaug==1.1.11
